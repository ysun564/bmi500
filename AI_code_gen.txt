AI-Assisted Code Generation Record
==================================

Date: 2026-09-16

LLM / tool used
-----------------
- Tool: OpenAI Codex desktop coding assistant
- Model: GPT-5 (Codex; the exact internal build identifier is not exposed)
- Supporting tools: PowerShell, Git, Python 3.9 standard-library unittest

Prompts supplied by the user
----------------------------

The prompts originally submitted in Chinese are translated into English below
so that the repository contains English text only.

Prompt 1:

Download my project into the BMI HW3 folder:
https://github.com/ysun564/bmi500

1. Implement the functions according to the comments in `matvec_multiply.py`.
2. The code may be written manually or with an LLM/coding assistant.
3. Write tests in `matvec_test.py`.
4. Analyze possible failure modes and add safeguards to the code, including:
   - mismatched matrix and vector dimensions
   - inputs that are not a two-dimensional matrix or one-dimensional vector
   - empty inputs
   - non-numeric inputs
   - irregular nested lists
5. Record the following in `AI_code_gen.txt`:
   - the LLM/tool used
   - the model version
   - all prompts
   - relevant conversation or output
6. Commit and push to your forked repository.

Prompt 2:

Okay.

Prompt 3:

Can you change the Chinese text on GitHub to English?

Prompt 4:

Okay.

Relevant conversation and output
--------------------------------

Codex inspected the repository and noted that its existing test skeleton is
named `matvec_tests.py` (plural). Codex proposed preserving and completing that
file, implementing the functions with plain Python loops, adding validation,
using test-driven development, documenting the AI interaction, and pushing to
the repository's `main` branch. The user approved that design.

The user later requested that all Chinese text in the GitHub repository be
changed to English and approved translating the prompt record.

TDD RED result before implementation:

    Ran 14 tests in 0.004s
    FAILED (errors=14)

All failures were the expected AttributeError errors because `dot_product` and
`matrix_vector_product` had not yet been implemented. A separate test for the
required `main` function also failed with the expected AttributeError before
that function was implemented.

TDD GREEN result after implementation:

    Ran 15 tests in 0.004s
    OK

Generated work
--------------
- Implemented `dot_product` with a for loop.
- Implemented `matrix_vector_product` using `dot_product`.
- Implemented `main`, which defaults to random 1000 x 1000 input data.
- Added validation for empty inputs, invalid dimensions, dimension mismatch,
  non-numeric values, and irregular matrix rows.
- Added 15 standard-library unit tests covering the implementation and guards.

No source code from another website or repository was copied into the solution.
